spring:
  application:
    name: query-server
server:
  port: 8088
management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  service-url:
    defaultZone: http://localhost:8001/eureka/