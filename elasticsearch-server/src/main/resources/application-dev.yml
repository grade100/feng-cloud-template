spring:
  application:
    name: elasticsearch-server
server:
  port: 8083
management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  service-url:
    defaultZone: http://localhost:8001/eureka/